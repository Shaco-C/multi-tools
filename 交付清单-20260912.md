# 随身工具箱交付清单（2026-09-12）

## 本包内容

- `android_app`：Kotlin + Jetpack Compose 原生 Android 多模块工程，是后续正式开发入口。
- `prototype`：用于对照界面与交互的前端原型，不是正式应用实现。
- `docs`：原生 Android、PWA 和 Flutter 三套方案文档。
- `另一台电脑使用说明.md`：笔记本电脑上的环境准备、测试和构建步骤。

## Android 工程当前状态

- 已建立 `app`、`core:common`、`core:designsystem`、`core:navigation`、`core:backup`、`feature:electricity` 模块。
- 已实现工具箱首页、电费分摊输入与计算、结果展示、退出未保存确认、删除分摊项确认等基础交互。
- 已加入金额精度、读数校验、月份校验以及核心计算和 ViewModel 测试源码。
- 已实现电费工具独立 Room 数据库，并把“保存本月账单”连接到本地事务写入。
- 已实现按月份倒序展示真实 Room 数据的历史账单列表。
- 已实现历史账单详情、修改回填、事务更新、删除确认与明细级联删除。
- 已实现只统计本人费用的月度趋势图、环比、平均值、最高值和月份明细。
- 已实现新账单按自然月自动带入上一期电表结构和读数，并避免覆盖手动输入。
- 已实现电费工具独立 JSON 备份、恢复预检、二次确认和事务替换。
- 已实现适合 Excel 查看分析的电费逐户 CSV 明细导出。
- 已在关键业务规则、事务边界、异步竞态和文件安全处理处补充中文维护注释。
- 已完成静态结构、依赖方向、Manifest 权限和敏感文件检查。
- 前端交互原型已通过 ESLint 与生产构建，并与 Android 统一为按余数补分的金额分摊算法。
- 因公司电脑未配置 Android SDK，尚未执行 Gradle 编译、单元测试、模拟器或真机验证。

## 到笔记本后的首轮验收

1. 按 `另一台电脑使用说明.md` 安装 Android 开发环境并打开 `android_app`。
2. 执行两个测试模块与 `:app:assembleDebug`。
3. 用 Android 模拟器和至少一台真机检查小屏、键盘弹出、返回键、旋转和深色模式。
4. Room、历史账单管理、趋势、自动带入、JSON 备份恢复和 CSV 导出均已接入；需在真机确认完整流程。

## 未打包内容

依赖目录、构建缓存、IDE 设置、Git 历史、本机 SDK 路径、签名密钥以及旧版本 ZIP 均被排除。
