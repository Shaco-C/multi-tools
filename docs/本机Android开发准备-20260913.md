# 本机 Android 开发准备（2026-09-13）

## 已验证结果

- Android Studio 已安装，产品版本为 AndroidStudio2026.1.3（build 261.26222.65.2613.15948027）。IDE 自带 JBR 25，项目构建使用已有 JDK 17.0.15；两者用途不同。
- Gradle 9.6.0 / AGP 9.4.0 / compileSdk 37 / Build-Tools 36.0.0 已完成实际构建。
- `:core:common:test`：5 项通过。
- `:feature:electricity:testDebugUnitTest`：51 项通过。
- `:app:assembleDebug`：通过，生成约 18.7 MiB 调试 APK。
- APK 权限检查未发现 INTERNET 权限；仅有 AndroidX 生成的应用内部动态接收器权限。
- APK SHA-256：`D59EF7B6828C96B703E19C7271630E2445FBAECD20B3399EDE928380427116D8`。
- ADB 37.0.1 已验证可运行，检查时没有连接真机或模拟器。Room 设备测试及真实界面交互尚未执行，JVM 测试不能替代它们。

## 本机路径

| 用途 | 路径 |
|---|---|
| Android Studio | `D:\Program Files (x86)\Android\Android Studio` |
| 打开工程的入口 | `D:\Program\multi-tools\android_app` |
| 原始源码 | `D:\Program\随身工具箱-原生Android源码与原型-20260912-v4` |
| Android SDK | `D:\Program\Android\Sdk` |
| Gradle 缓存 | `D:\Program\gradle\repo` |
| 构建 JDK | `D:\jdk17\jdk-17.0.15` |
| Android 用户数据 | `D:\Program\Android\user-home` |
| 模拟器数据预留目录 | `D:\Program\Android\avd` |
| Studio 配置/缓存/插件/日志 | `D:\Program\Android\studio` 下的独立子目录 |
| 临时文件 | `D:\Program\Android\temp` |

`D:\Program\multi-tools` 是指向原始源码目录的 Junction，不是第二份副本。Windows 下 Android 插件拒绝中文工程路径，因此后续请使用这个英文入口。C 盘原来的 `.android` 和 `.gradle` 也已重定向到 D 盘，旧路径可继续使用而不把实际 Android/Gradle 数据存到 C 盘。

## 首次打开

关闭旧 Android Studio 后重新打开，使它读取新的用户环境变量。用 Android Studio 打开 `D:\Program\multi-tools\android_app`。

- SDK 路径在忽略的 `local.properties` 中指定。
- 用户 `STUDIO_GRADLE_JDK` 与工程忽略的 `.gradle/config.properties` 指向 JDK 17。
- 用户 `STUDIO_PROPERTIES` 指向 `D:\Program\Android\studio.properties`。
- 用户 `STUDIO_VM_OPTIONS` 指向 `D:\Program\Android\studio.vmoptions`，内容基于本次安装自带默认值，避免继承原来第三方的启动配置。
- `ANDROID_HOME`、`ANDROID_USER_HOME`、`ANDROID_EMULATOR_HOME`、`ANDROID_AVD_HOME`、`GRADLE_USER_HOME` 已配置到 D 盘。

## 命令行构建

在新的 PowerShell 中执行：

```powershell
$env:JAVA_HOME = 'D:\jdk17\jdk-17.0.15'
$env:GRADLE_USER_HOME = 'D:\Program\gradle\repo'
$env:ANDROID_HOME = 'D:\Program\Android\Sdk'
$env:ANDROID_USER_HOME = 'D:\Program\Android\user-home'
$env:TEMP = 'D:\Program\Android\temp'
$env:TMP = $env:TEMP
cd D:\Program\multi-tools\android_app
.\gradlew.bat :core:common:test :feature:electricity:testDebugUnitTest :app:assembleDebug --max-workers=4
```

APK 位于 `android_app/app/build/outputs/apk/debug/app-debug.apk`，被 Git 忽略。正式签名密钥不要提交。

## 下一步设备验收

连接开启 USB 调试的安卓手机，在手机上允许此电脑调试，然后运行 SDK 中的 `platform-tools/adb.exe devices -l` 确认设备。Android Studio 中选择手机并运行 app。也可之后安装 Emulator 和系统镜像，模拟器数据路径已预留到 D 盘；本次没有安装大型系统镜像。

设备就绪后执行：

```powershell
.\gradlew.bat :feature:electricity:connectedDebugAndroidTest --max-workers=4
```

再检查账单保存/修改/删除、上月读数带入、趋势、JSON 备份恢复、CSV 导出、小屏键盘、返回手势与旋转。

## 本次源码修改与已知事项

- Google Maven 的默认 `/dl/android/maven2` 地址在本机超时，改用验证可访问的同一官方域名 `/android/maven2/` 地址；没有引入第三方镜像或关闭 TLS 检查。
- 修复金额格式正则过早拒绝三位小数的问题，保留精度校验，让 `1.001` 正确返回“金额最多保留两位小数”。现有测试已覆盖并通过。
- 提交首次真实构建生成的 Room v1 schema，便于后续数据库迁移。
- 编译仍有少量图标 API 弃用提示；debug APK 中一项第三方 native 库未剥离符号，不影响本次打包成功。
- SDK Command-line Tools 23 已转发到新版 Android CLI，包名使用 `platforms/android-37.0`。安装器解压结束时曾异常退出，但平台/工具元数据、ADB、aapt2 和完整 APK 构建均已验证。
- 原型缺少 `prototype/build/sites-vite-plugin` 的交付问题仍独立存在，不影响本次原生 Android 工程验证。

历史交付文档中“尚未编译”的表述描述的是原始交付时状态；本机当前结果以本记录为准。
