# 本机迁移检查（2026-09-13）

## 已确认

- 正式目录是 `android_app`，不是 `android/_app`。
- 已阅读原生方案、交付清单、另一台电脑使用说明和静态复核记录，并检查模块配置、分摊算法、Room Repository 与原型入口。
- 原生工程有六个模块，业务数据归电费模块独立管理。历史交付记录中的测试通过情况不能代表本机验证结果。
- Git 已安装，提交身份已有配置。
- 默认 Java/JAVA_HOME 为 JDK 21.0.7；另有可运行的 JDK 17.0.15：`D:\jdk17\jdk-17.0.15`。`core:common` 显式要求 `jvmToolchain(17)`。
- 常见安装路径、环境变量和 Windows 卸载记录中未找到 Android Studio 或 Android SDK。自定义安装位置仍可能存在。
- PATH 中 adb 来自 scrcpy，不能据此认为已安装完整 Android SDK。
- Node.js 为 20.11.1，npm 为 10.9.1；原型 package.json 要求 Node.js >=22.13.0，当前默认 Node 不满足。
- 原型 vite.config.ts 引用 `./build/sites-vite-plugin`，交付包中缺少此文件，安装 npm 依赖不会补回这份本地源码。
- 文件名和常见密钥格式扫描未发现需排除的现存密钥；不是全面安全审计。

## 接续开发

1. 确认或补齐 Android Studio、工程要求的 SDK Platform、Build-Tools、Platform-Tools；需要模拟器时再配置 Emulator/系统镜像。
2. Android Studio 的 Gradle JDK 指向上面的 JDK 17。命令行可在当前 PowerShell 会话设置 `$env:JAVA_HOME = 'D:\jdk17\jdk-17.0.15'`，无需修改全局 Java。
3. SDK 路径通过 IDE 生成的 android_app/local.properties 设置，该文件已忽略。
4. 按 README 执行 Wrapper、单元测试、APK 构建和设备数据库测试；依赖版本解析与兼容性以实际构建为准。
5. 若需要运行原型，先准备满足 engines 的 Node，再从原电脑找回 build/sites-vite-plugin 及其依赖源码，或单独改造为本地原型构建。

## 提交边界

根目录 .gitignore 覆盖 Android 和前端生成物、IDE 状态、签名材料、环境文件、设备数据库及导出目录。保留 Gradle Wrapper JAR、package-lock.json、Room schemas、原型源素材与第三方许可证。原型 .openai/hosting.json 被构建配置引用，内容是项目标识和空资源绑定，作为已有构建配置保留。

本次不将缓存、SDK、签名材料或测试账单上传仓库，不把静态审阅表述为编译验收通过。
