# 另一台电脑使用说明

这个项目用于在没有个人 Git 账号的电脑之间继续开发，不包含 Git 历史、已安装依赖和构建缓存。

## 原生 Android 开发环境（正式路线）

- Android Studio（建议使用当前稳定版）。
- Android SDK 37。
- JDK 17，并在 Android Studio 中将 Gradle JDK 指向它。

不需要登录 Git；直接解压并用 Android Studio 打开 `android_app` 目录即可。项目已经包含 Gradle Wrapper，不需要单独安装 Gradle。

首次打开后，依次执行：

```powershell
cd android_app
.\gradlew.bat --version
.\gradlew.bat :core:common:test
.\gradlew.bat :feature:electricity:test
.\gradlew.bat :app:assembleDebug
```

生成的调试 APK 位于：

```text
android_app/app/build/outputs/apk/debug/app-debug.apk
```

公司电脑没有 Android SDK，因此当前代码只完成了静态复核，尚未实际编译和安装运行。接手时请先阅读 `android_app/STATIC_REVIEW.md`，再执行上面的测试和构建命令。

## 浏览交互原型（可选）

只有需要对照早期交互原型时，才需要安装 Node.js 22 LTS（含 npm）以及 Chrome 或 Edge。Visual Studio Code 可选。

## 启动原型

解压后，在 `prototype` 目录打开 PowerShell 或终端：

```powershell
npm ci
npm run dev
```

浏览器打开：

```text
http://localhost:5173
```

在 Chrome 或 Edge 中按 F12，点击设备工具栏按钮，即可切换到手机尺寸。

## 生成生产构建

```powershell
npm run build
```

## 文件说明

- `android_app`：正式实施的 Kotlin 原生 Android 多模块源码。
- `docs`：原生安卓、PWA 前端和 Flutter 三套方案设计文档。
- `prototype`：当前可交互原型源代码。
- `交付清单-20260912.md`：本次压缩包的内容、状态和接手检查项。
- `prototype/node_modules`：不会打入压缩包；在新电脑执行 `npm ci` 后重新生成。
- `prototype/.git`：不会打入压缩包，因此不需要公司电脑登录个人 Git。
- `prototype/dist`、`.next`、`.wrangler`：构建缓存不会打包，可随时重新生成。

## 数据提醒

当前原型的历史记录和趋势为演示数据，保存按钮只展示交互状态。正式开发加入本地数据库后，换电脑应同时导出需要迁移的测试数据。

## Flutter 方案说明

当前压缩包尚不包含 Flutter 工程；`docs/方案C-Flutter跨平台应用设计方案.md` 是后续初始化工程时使用的设计依据。安装 Flutter SDK 和 Android 工具链后，再按照该文档创建 `flutter_app` 多包工作区。

## 打包边界

压缩包不包含 `node_modules`、`.git`、`.idea`、`.gradle`、各模块的 `build`、`local.properties`、签名文件和旧 ZIP。这些内容要么可以重新生成，要么不适合在电脑之间传递。
